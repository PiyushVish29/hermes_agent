# Configuration

This directory is reserved for checked-in, non-secret configuration templates. Runtime settings currently come from the small allowlist in `app/config/settings.py` and optional `.env`-style environment variables supplied by the host.
